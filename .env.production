# R36 shared Himay tracking pixel (not a secret, same value already set as
# the server-side META_PIXEL_ID Pages env var used by functions/api/meta-events.ts).
# Added here for consistency/future-readiness (HIM-356): this repo has no Meta
# Pixel client component at all (GTM only), so this alone does not activate
# anything. Wiring a full Pixel+CAPI pair from scratch is out of scope here.
NEXT_PUBLIC_META_PIXEL_ID=1837485590560640
